{
  "name": "my-webpack-project",
  "version": "1.0.0",
  "description": "My webpack project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./pick-dom-element/ && npm run build && cd .. && webpack --mode=production --node-env=production",
    "build:dev": "cd ./pick-dom-element/ && npm run build && cd .. && webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "watch": "webpack --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@medv/finder": "^2.0.0",
    "lodash": "^4.17.21",
    "pick-dom-element": "file:pick-dom-element"
  },
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/preset-env": "^7.15.0",
    "@webpack-cli/generators": "^2.2.0",
    "babel-loader": "^8.2.2",
    "webpack": "^5.49.0",
    "webpack-cli": "^4.7.2"
  }
}
